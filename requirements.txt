# MoEFCC NLP Pipeline — Requirements
# Install with: pip install -r requirements.txt

# PDF extraction (all 3 needed — pipeline picks the best one per PDF)
PyMuPDF>=1.23.0
pdfminer.six>=20221105
pdfplumber>=0.10.0

# NLP
spacy>=3.7.0
# After install run: python -m spacy download en_core_web_sm

# Machine learning
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0

# Charts
matplotlib>=3.7.0
seaborn>=0.12.0
